Its a simple rails application you just need to clone application then need to run rails server
by rails s
and open browser and type http://localhost:3000/
where you can see a text field used for enter numbers to convert them to words.

Also there are a simpme test cased for all those examples.
